# Secrets and credentials — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
credentials.json
credentials.csv
aws-credentials
.aws/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Dependency directories (if application source is added later)
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build output
dist/
build/
.next/

# Infrastructure-as-code local state (if IaC is added later)
*.tfstate
*.tfstate.*
.terraform/

# Local scratch / temp files
*.tmp
*.log

# Editor-generated diagram previews (auto-exported by draw.io editor extensions)
docs/architecture/*.drawio.png

# Project walkthrough video — too large for a normal git push (168MB > GitHub's
# 100MB hard limit) and hosted externally instead. See README.md / docs/demo-script.md
# for the link. Keeping the raw file here locally is fine; it just won't be pushed.
final.mp4
docs/video/
